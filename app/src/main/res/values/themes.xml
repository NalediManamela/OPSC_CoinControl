<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OPSC_CoinControl" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorSecondary">@color/teal_200</item>
    </style>

    <style name="Theme.OPSC_CoinControl" parent="Base.Theme.OPSC_CoinControl" />

    <style name="Theme.OPSC_CoinControl.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.OPSC_CoinControl.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.OPSC_CoinControl.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>